import { Skill } from "../components/organisms";

const SkillSection = () => {
  return (
    <section id="skill" className="relative">
      <Skill />
    </section>
  );
};

export default SkillSection;
